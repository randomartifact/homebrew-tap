# LLM Status Homebrew tap

```sh
brew install randomartifact/tap/modelstatus-cli
```

Installs `mm` (and a `llmstatus` alias) — the LLM Status CLI that tracks which AI
models your codebase uses and warns before they retire. The macOS binary is
Developer ID signed and notarized by Apple. Source: https://llmstatus.ai
